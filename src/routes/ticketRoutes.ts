// src/routes/ticketRoutes.ts
import { Router } from 'express';

//import { TicketController } from '../api/controllers/TicketController';

const router = Router();
//const ticketController = new TicketController();

// Rota para obter todos os filmes
/*

router.post('/movies/{movie_id}/sessions/{session_id}/tickets' , ticketController.postTicket.bind(ticketController));
router.put('/movies/{movie_id}/sessions/{session_id}/tickets/:id , ticketController.putTicket.bind(ticketController));
router.delete('/movies/{movie_id}/sessions/{session_id}/tickets/:id' , ticketController.deleteTicket.bind(ticketController));

*/

export default router;
